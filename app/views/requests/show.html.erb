<h2 class="headline-title"><%= image_tag("apps/#{@webhook.name}.png", style: 'width: 30px;margin-top: -5px;margin-right: 5px') %> <%= appSingle(@webhook.name)['displayName'] %></h2>

<div class="webhook-options clearfix">
  <a href="/webhooks/<%= @webhook.slug %>/edit" class="btn btn-outline-light float-right"><i class="fas fa-edit mr-0"></i></a>
  <a href="/export?id=<%= @webhook.id %>" download class="btn btn-outline-light mr-2 float-right"><i class="fas fa-download"></i> CSV</a>
  <select id="activity-date-picker" data-url="<%= request.base_url %>/database/<%= @webhook.slug %>" class="form-control float-right mr-2">
    <option value="?timeframe=7-days">Last 7 Days</option>
    <option value="?timeframe=30-days" selected>Last Month</option>
    <option value="?timeframe=90-days">Last 3 Months</option>
    <option value="?timeframe=1-year">Last Year</option>
    <option value="?timeframe=all-time">All Time</option>
  </select>
</div>

<hr>

<div class="card-block chartjs mb-3">
  <%= line_chart @request_activity, height: '200px' %>
</div>


<div id="search-table" class="float-right">
  <input type="search" autocomplete="off" class="form-control form-control-sm" id="search" placeholder="Search <%= @webhook.name %>..." data-type="<%= @webhook.slug %>" />
</div>

<%= render 'shared/table_controls' %>

<div class="searching-for text-muted mb-3 badge badge-secondary">
  Searching for "<strong>Search Term</strong>" with <span><%= @search_count %></span> result(s).
</div>

<%= @results.html_safe %>

<%= will_paginate @requests, class: 'table_pagination mt-3 mb-3' %>

<%= form_for '/delete_requests', html: {id: 'delete-requests', class: 'hide'} do |form| %>
  <input type="text" name="delete_ids" />
  <input type="text" name="redirect" value="<%= request.url %>" />
<% end %>

<div id="popup-wrapper" style="display: none">
  <div id="popup" class="card-block">
    <p class="text-muted">You can use the bold text as key for Table columns.</p>
    <div id="row-data-popup"></div>
  </div>
</div>
